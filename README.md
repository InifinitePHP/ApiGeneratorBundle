# ApiGeneratorBundle

A Symfony-based API to generate resources very fast.

Lancer les tests :
```
XDEBUG_MODE=coverage php bin/phpunit --coverage-html=.phpunit.cache/coverage
php -d memory_limit=-1 vendor/bin/phpstan analyse -l 10
```